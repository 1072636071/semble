# jxx-agent-generator

按"姜姓身份 + 统一 frontmatter + 既有写作风格"生成或改造 CodeBuddy agent 文件。

## 何时使用

- 用户想新建 agent、给 agent 加身份设定（姓姜 + 扮演角色）、或统一一批 agent 的风格/规范时使用。
- 触发词："生成一个 agent""创建 agent""改造这个 agent""给 agent 加身份""统一 agent 风格"。

## 产物

生成或改造后的 agent 文件，位于 `~/.codebuddy/agents/名字.md`。
