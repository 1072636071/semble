# jxx-improve-codebase-architecture

扫描代码库（codebase）寻找深化（deepening）机会，以可视化 HTML 报告呈现，再 grill 你选中的那一个。

## 何时使用

发现架构摩擦并提出深化机会——将浅模块变为深模块的重构，目标是可测试性和 AI 可导航性。

## 依赖

- 依赖 `jxx-codebase-design` 技能提供的架构词汇与共享设计词汇。
- 依赖项目中的 `CONTEXT.md` 领域语言与 `docs/adr/` 中的 ADR 记录。
