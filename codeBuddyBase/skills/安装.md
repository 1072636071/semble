# 技能安装指南

将以下两个目录中的技能安装到各平台全局技能目录，已安装则覆盖。

## 源目录

| 源目录 | 路径 |
|--------|------|
| engineering | `E:\work\sp\JwikisSkills\三方\codeBuddy\skills\engineering` |
| productivity | `E:\work\sp\JwikisSkills\三方\codeBuddy\skills\productivity` |

## 目标目录

| 平台 | 路径 |
|------|------|
| CodeBuddy | `C:\Users\jxc123\.codebuddy\skills` |
| OpenCode | `C:\Users\jxc123\.agents\skills` |
| 码道 | `C:\Users\jxc123\.codeartsdoer\skills` |
| Qoder | `C:\Users\jxc123\.qoder-cn\skills` |
| Trae | `C:\Users\jxc123\.trae-cn\skills` |
| WorkBuddy | `C:\Users\jxc123\.workbuddy\skills` |
| QoderWork | `C:\Users\jxc123\.qoderworkcn\skills` |

## 安装方式

将源目录下每个子目录（即每个技能）复制到目标目录下，覆盖同名目录。**engineering 全部安装**（`jxx-*` 通配）；**productivity 仅安装默认列表**（`jxx-teach`、`jxx-wait-what`、`skill-creator`、`skill-reviewer`、`skill-tester` 除外）。脚本同时**删除目标中已从源移除的残留技能**（如 `jxx-writing-great-skills`），保证新旧技能同步。

```powershell
$srcEng = "E:\work\sp\JwikisSkills\三方\codeBuddy\skills\engineering"
$srcProd = "E:\work\sp\JwikisSkills\三方\codeBuddy\skills\productivity"

# productivity 默认安装列表——新增默认技能时在此追加
$prodSkills = @(
    "jxx-grill-me"
    "jxx-grilling"
    "jxx-handoff"
    "jxx-to-questionnaire"
    "jxx-writing-for-agents"
)

$targets = @(
    "C:\Users\jxc123\.codebuddy\skills"
    "C:\Users\jxc123\.agents\skills"
    "C:\Users\jxc123\.codeartsdoer\skills"
    "C:\Users\jxc123\.qoder-cn\skills"
    "C:\Users\jxc123\.trae-cn\skills"
    "C:\Users\jxc123\.workbuddy\skills"
    "C:\Users\jxc123\.qoderworkcn\skills"
)

foreach ($dest in $targets) {
    # 1) engineering：复制全部 jxx-* 技能
    Get-ChildItem "$srcEng\jxx-*" -Directory | ForEach-Object {
        Copy-Item -Path $_.FullName -Destination $dest -Recurse -Force
    }
    # 2) productivity：复制默认安装列表
    foreach ($skill in $prodSkills) {
        $src = "$srcProd\$skill"
        if (Test-Path $src) { Copy-Item -Path $src -Destination $dest -Recurse -Force }
    }
    # 3) 清理残留：目标中存在、但两个源目录都不再有的 jxx-* 技能目录
    #    （同时检查 engineering 与 productivity 源，避免误删源中仍存在的技能）
    Get-ChildItem $dest -Directory -Filter "jxx-*" | ForEach-Object {
        $inEng = Test-Path "$srcEng\$($_.Name)"
        $inProd = Test-Path "$srcProd\$($_.Name)"
        if (-not $inEng -and -not $inProd) {
            Remove-Item $_.FullName -Recurse -Force
            Write-Host "  cleaned: $($_.Name)"
        }
    }
}
```

## 技能清单

### engineering（26 个技能 + 1 个 README + 1 个工作流说明）

| 技能名 | 说明 |
|--------|------|
| jxx-agent-generator | Agent 生成器 |
| jxx-ask-matt | 向 Matt Pocock 提问（路由器） |
| jxx-code-review | 代码审查（标准 + spec 双维度） |
| jxx-codebase-design | 深模块接口设计 |
| jxx-design-system | 设计系统（DESIGN.md） |
| jxx-diagnosing-bugs | Bug 诊断循环（含脱敏） |
| jxx-domain-modeling | 领域建模 |
| jxx-goal-contract | 目标契约固化（GOAL.md） |
| jxx-goal-execute | 目标自主执行循环 |
| jxx-goal-mode | 目标模式 |
| jxx-grill-with-docs | 带文档追问 |
| jxx-grill-with-memorial | 带奏报持久化的追问（支持中断续接、调查委派、收尾审核） |
| jxx-implement | 实现 |
| jxx-improve-codebase-architecture | 改进代码库架构 |
| jxx-loop-me | 循环模式设计 |
| jxx-plan-review | 计划审查 |
| jxx-prototype | 原型构建（逻辑分支为 HTML demo） |
| jxx-research | 调研/研究报告 |
| jxx-resolving-merge-conflicts | 解决合并冲突 |
| jxx-setup-matt-pocock-skills | 安装 Matt Pocock 技能 |
| jxx-tdd | 测试驱动开发 |
| jxx-to-spec | 转规格说明 |
| jxx-to-tickets | 转工单 |
| jxx-triage | 分类 |
| jxx-wayfinder | 路径查找 |
| jxx-wizard | 手动流程向导生成 |

### productivity（5 个技能，默认安装）

| 技能名 | 说明 |
|--------|------|
| jxx-grill-me | 追问我 |
| jxx-grilling | 追问（round-by-round frontier 访谈） |
| jxx-handoff | 交接 |
| jxx-to-questionnaire | 转问卷 |
| jxx-writing-for-agents | 为 agent 写文档 |

### productivity（5 个技能，默认不安装）

| 技能名 | 说明 |
|--------|------|
| jxx-teach | 教学 |
| jxx-wait-what | 简化技术英语重讲 |
| skill-creator | 技能创建器 |
| skill-reviewer | 技能评审器 |
| skill-tester | 技能测试器 |

## 验证

安装完成后，对所有平台验证本次修改过的技能是否部署正确：

```powershell
$srcEng = "E:\work\sp\JwikisSkills\三方\codeBuddy\skills\engineering"
$srcProd = "E:\work\sp\JwikisSkills\三方\codeBuddy\skills\productivity"

# 本次新增/修改的技能（根据 git diff 维护）
$modifiedSkills = @(
    @{ Name = "jxx-wizard";            Src = $srcEng }
    @{ Name = "jxx-ask-matt";          Src = $srcEng }
    @{ Name = "jxx-diagnosing-bugs";   Src = $srcEng }
    @{ Name = "jxx-prototype";         Src = $srcEng }
    @{ Name = "jxx-code-review";       Src = $srcEng }
    @{ Name = "jxx-codebase-design";   Src = $srcEng }
    @{ Name = "jxx-improve-codebase-architecture"; Src = $srcEng }
    @{ Name = "jxx-grilling";          Src = $srcProd }
    @{ Name = "jxx-to-questionnaire";  Src = $srcProd }
    @{ Name = "jxx-writing-for-agents";Src = $srcProd }
)

$targets = @(
    "C:\Users\jxc123\.codebuddy\skills"
    "C:\Users\jxc123\.agents\skills"
    "C:\Users\jxc123\.codeartsdoer\skills"
    "C:\Users\jxc123\.qoder-cn\skills"
    "C:\Users\jxc123\.trae-cn\skills"
    "C:\Users\jxc123\.workbuddy\skills"
    "C:\Users\jxc123\.qoderworkcn\skills"
)

foreach ($dest in $targets) {
    $label = Split-Path $dest -Parent | Split-Path -Leaf
    Write-Host "=== $label ==="
    foreach ($skill in $modifiedSkills) {
        $srcFile = "$($skill.Src)\$($skill.Name)\SKILL.md"
        $dstFile = "$dest\$($skill.Name)\SKILL.md"
        if (Test-Path $dstFile) {
            $srcHash = (Get-FileHash $srcFile -Algorithm SHA256).Hash
            $dstHash = (Get-FileHash $dstFile -Algorithm SHA256).Hash
            if ($srcHash -eq $dstHash) { Write-Host "  $($skill.Name) : OK" }
            else { Write-Host "  $($skill.Name) : MISMATCH" }
        } else {
            Write-Host "  $($skill.Name) : MISSING"
        }
    }
}
```

若 Copy-Item 报 `untrusted mount point` 错误，改用 robocopy：

```powershell
$destBase = "C:\Users\jxc123\.agents\skills"
$srcEng = "E:\work\sp\JwikisSkills\三方\codeBuddy\skills\engineering"
$srcProd = "E:\work\sp\JwikisSkills\三方\codeBuddy\skills\productivity"

# engineering
Get-ChildItem "$srcEng\jxx-*" -Directory | ForEach-Object {
    robocopy $_.FullName "$destBase\$($_.Name)" /E /IS /IT /NFL /NDL /NJH /NJS
}
# productivity
@("jxx-grill-me", "jxx-grilling", "jxx-handoff", "jxx-to-questionnaire", "jxx-writing-for-agents") | ForEach-Object {
    robocopy "$srcProd\$_" "$destBase\$_" /E /IS /IT /NFL /NDL /NJH /NJS
}
```

## 注意

- 不要复制 `README.md` 和 `正常的工作流.md`（非技能目录）
- 安装后重启各平台或重新加载技能列表
- 源目录中的 `.md`、`.json`、`.txt`、`.py`、`.sh` 等附属文件一并复制，技能可能依赖
- 每次修改技能源文件后，执行安装 + 验证，确保所有平台同步
- 若某技能已从源删除，安装脚本会清理目标中的残留目录；如需保留旧版，手动从目标移出
