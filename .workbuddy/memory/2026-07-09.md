# 2026-07-09 工作日志

## 新建技能：jxx-goal-mode（目标模式）

在 `三方/mattpocock-skills-A/skills/engineering/` 下新建技能目录 `jxx-goal-mode/`。

**核心设计**：以结果为导向的自主执行循环。用户只定"目标-验收标准-约束"三元组（固化到 `GOAL.md` 契约），AI 自主拆解→执行→自检→循环至达标再交付（早C晚A）。

**技能文件**：
- `SKILL.md` — 4 阶段流程（固化契约 / 拆解 / 执行循环 / 自检交付）+ 护栏 + 反模式 + 适用边界
- `references/goal-contract.md` — GOAL.md 契约模板与示例
- `references/acceptance-patterns.md` — 验收三性质（可观测/可判定/独立来源）+ 好坏对照
- `references/constraint-design.md` — 边界 vs 步骤的区分
- `README.md` / `CHANGELOG.md` / `LICENSE.txt`
- `docs/engineering/goal-mode.md` — 面向人类的文档页面（按 writing-docs.md 模板）

**定位**：user-invoked（`disable-model-invocation: true`），因为是否启动自主循环只能由人决定。

**已同步索引**：engineering/README.md（用户调用区）、顶层 README.md、.claude-plugin/plugin.json。

**仓库规范要点（后续在此仓库加技能时遵循）**：
- 目录名用 `jxx-` 前缀；plugin.json 用无前缀路径（`./skills/engineering/<name>`）；README/docs 链接用 `jxx-` 目录。
- 推广桶（engineering/productivity）技能必须同步：桶 README + 顶层 README + plugin.json + docs 页面。
- 技能分 user-invoked（`disable-model-invocation: true`，描述面向人类）与 model-invoked（默认，描述含触发词）。
- 跨技能调用用 `/jxx-skill` 风格，不跨文件夹引用文件。
- 写法：祈使句，阶段化结构 + checkbox 完成标准 + 反模式章节。

## 新建技能：design-system（DESIGN.md 驱动的 UI 一致性）

在 `Skills技能库/工具包/码道的/系统级/` 下新建技能目录 `design-system/`。基于 Google [design.md](https://github.com/google-labs-code/design.md) 格式。

**核心设计**：项目级设计系统确立与跨生成一致性维护。`DESIGN.md` 作为单一设计数据源（YAML 设计令牌 + Markdown 理念）。项目开始时引导询问（风格预设/配色基调/技术栈/品牌约束），从 10 种预设选起点定制生成 `DESIGN.md`，后续每次生成 UI 都引用令牌不硬编码。

**技能文件**：
- `SKILL.md` — 5 步工作流（检测启动 / 引导询问 / 生成 DESIGN.md / 一致性维护 / 验证导出）+ 10 风格索引 + 反模式
- `references/design-md-spec.md` — DESIGN.md 格式规范（token schema / section 顺序 / 9 条 lint 规则）
- `references/lint-export.md` — CLI 工具链（lint / diff / export / spec）
- `references/themes/01-10-*.md` — 10 种主流风格预设（每个是完整 DESIGN.md：Material 3 / Apple HIG / Fluent / Linear / Minimalist Modern / Cyberpunk / Skeuomorphism / Glassmorphism / Neo-Brutalism / Shadcn-Tailwind）
- `assets/DESIGN.template.md` — 空白模板
- `README.md` / `CHANGELOG.md` / `LICENSE.txt`

**定位**：model-invoked（默认，description 含触发词：配色/UI风格一致性/DESIGN.md/design system 等）。

**验证**：`quick_validate.py` 通过（"Skill is valid!"）。frontmatter 仅 name + description + metadata.version。

**已同步**：`SystemSkillStatus.txt` 加 `design-system=true`（共 12 个系统技能）。

**与 frontend-design 协作**：frontend-design 在令牌边界内生成美观代码，design-system 提供令牌边界。

**仓库规范要点（JwikisSkills 码道系统级技能）**：
- name 须 kebab-case 且匹配目录名；frontmatter 仅允许 name/description/license/allowed-tools/metadata/compatibility；description ≤1024 字符。
- 必备 SKILL.md + README.md + CHANGELOG.md + LICENSE.txt；可选 references/ assets/ scripts/ tests/ evals/。
- 系统级技能在 `SystemSkillStatus.txt` 登记。
- 验证用 skill-creator 的 `scripts/quick_validate.py`（需 pyyaml，用 managed python venv 运行）。
- 渐进式披露：SKILL.md <500 行，详细内容拆 references/。

## 新建技能：jxx-design-system（mattpocock engineering 桶）

在 `三方/mattpocock-skills-A/skills/engineering/` 下新建 `jxx-design-system/`，基于上一步 JwikisSkills 系统级 design-system 复制改造。同份设计系统技能在两个仓库各有一份：JwikisSkills 系统级（model-invoked，配 SystemSkillStatus）+ mattpocock engineering（jxx- 前缀，配 README/plugin.json/docs/ask-matt）。

**改造点（mattpocock 版 vs JwikisSkills 版）**：
- frontmatter: name 改 `jxx-design-system`；description 中文化贴合仓库风格；model-invoked（无 disable-model-invocation）。
- 跨技能调用改 `/jxx-` 风格；mattpocock 无 frontend-design，协作改为 `/jxx-prototype`、`/jxx-implement`；追问纪律改 `/jxx-grill-me`。
- 新增 `docs/engineering/design-system.md`（writing-docs 模板，无 H1、绝对链接）。

**已同步索引**：engineering/README.md（模型调用区）、顶层 README.md、.claude-plugin/plugin.json（现 22 个）、ask-matt SKILL.md（独立技能区，prototype 后加 design-system 条目，说明与 prototype/implement 协作）。

**验证**：quick_validate.py 通过。两个仓库的 design-system 内容一致（10 主题 + 规范 + CLI 用法 + 模板），仅 frontmatter/调用风格/索引机制按各自仓库规范。
